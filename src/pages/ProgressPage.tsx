import { StyleSheet, Text, View } from "react-native"

const Progress =  () => {
    return(
        <View style={styles.container}>
            <Text>hello theo doi tien do </Text>
        </View>
    )
}
const styles = StyleSheet.create({
    container: {
        flex: 1,
        justifyContent: "center",
        alignItems: "center"
    }
})
export default Progress;